# One command: docker compose up --build
# App URL: http://localhost:8081/WebApp/

services:
  webapp:
    build: .
    image: webapp:latest
    container_name: webapp-tomcat
    ports:
      - "8081:8080"
    restart: unless-stopped
